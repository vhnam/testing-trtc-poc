'use client';

import AppointmentsContainer from '@/modules/appointments/appointments-container';

const AppointmentsPage = () => {
  return <AppointmentsContainer />;
};

export default AppointmentsPage;
